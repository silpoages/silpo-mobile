/** Raios de borda das telas de autenticação, como no Figma. */
export const radii = {
  /** Campos, botão do Google e botão de voltar. */
  field: 12,
  /** Botões principais. */
  button: 14,
  /** Selo de apoio e distintivo do Google. */
  pill: 999,
} as const;
